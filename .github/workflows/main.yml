name: Main

on:
  # weekly
  schedule:
    - cron: 0 0 * * 0
  # on-demand
  repository_dispatch:

jobs:
  build:
    env:
      branch-to-build: ${{ github.event.client_payload.branch || 'master' }}
    name: Build for the ${{ jobs.build.branch-to-build }} branch
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: ${{ jobs.build.branch-to-build }}
    - name: Run index.js
      run: node index.js
